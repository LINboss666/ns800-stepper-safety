# 待办事项（唯一权威清单）

> 怕漏东西就看这个文件。每条完成就地打勾并注明日期/commit。
> 状态：🔴 高优先（安全/阻塞） · 🟡 中优先（功能验证） · 🟢 低优先（材料/远期）
> 更新：2026-09-13

## 🔴 高优先

- [ ] **Phase 7 on-target 冒烟**（板连上后第一件事）：刷 phase7 分支固件 → `runtime_selftest`(应 ALL PASS) → `diag_selftest`(应 ALL PASS) → `system_status` → `blackbox_selftest`(写真实 Flash) → `sensor_snapshot`/`current_raw`

- [ ] **ENN 浮空行为实验**（安全相关）：流程=先拔 VM → 拔 ENN 线 → `tmc_status` 看 IOIN bit0 → 判定浮空=使能还是禁用。背景：现在 VM 有电+电机已接，ENN→3.3V 这根线是唯一安全屏障，必须插牢（建议胶带固定）；紧急停机=拔 VM，勿拔 ENN
- [ ] **J4-21 万用表终验**：新使能脚 PC.23/MCU_DRV_ENABLE 上电应稳定 0V（原 PF.21 坏脚弃用，BUG-009）
- [ ] **安全输入上拉代码改动**（等用户点头）：safety_gpio.c 中 ESTOP/J4-40 输入改 `PIN_MODE_INPUT_PULLUP`（NC+上拉失效安全方案）；限位暂不接开关→J4-19/J4-25 各插一根 **GND 跳线**占位（防浮空误报触发）
- [ ] **软件急停补齐**（等用户点头，二选一或全做）：①最小版=新增 `estop` msh 命令（调 safety_force_shutdown，进 SAFETY_ESTOP 状态）②完整版=ESTOP_SENSE(PC.6/J4-40) EXTI6 中断 → Safety Thread(优先级4) → 自动停机
- [ ] **INPUT_SAFE_LEVEL_xxx 宏冻结**：safety_state.c 安全电平按实际接线（NC+上拉方案下 safe=0 恰好正确，接线确认后正式冻结）

## 🟡 中优先

- [x] 2026-09-13 **用户已自搭 ADC 采样硬件并接到 J1-3**，DIAG 也已接 → 升级为"电机+ADC 联合测试"
- [ ] **⏸ 电机+ADC 联合测试（已暂停，方案已备，等用户下发指令继续）**：四级方案=阶段0 零电流基线(ENN保持3.3V,current_raw×3 记零点)→阶段1 STEP注入(仍禁用,验证互不干扰)→阶段2 使能+静止维持电流(先写最小 IRUN≈50-100mA, ENN 挪 GND, current_raw, <10s, 回禁用)→阶段3 低速 pwm_test 20+电流联测。**开跑前等用户确认4项**：①VM 电压/限流值 ②用户ADC前端拓扑(分流/增益/零点) ③电机轴是否自由 ④批准 tmc2209.c 加 tmc_current 命令(写 IHOLD_IRUN 钉最小电流,使能前最重要的安全措施)。门禁缺口如实声明：无示波器验收 STEP+ESTOP/限位未就绪,缓解=最小电流+低速+短时+用户守 VM 开关
- [ ] **电机受控使能路径**（接电机转动实验的前置）：当前 ENN 直挂 3.3V=软件无法使能电机。可选方案：a) ENN 手动线控（杜邦线接 GND=使能/3.3V=禁用）b) 用空闲 GPIO 驱动 ENN（GPIO_86/GPIO_90 因位拍回滚已闲置；引脚冻结表变更需用户确认）。转动实验门禁：12V 限流 + 低频低速 + 空载 + STEP 频率尽可能验证
- [ ] **IMU INT1 (PA.21/J2-15) 中断功能验证**（EXTI5，线已接未测）
- [ ] **Phase 7 主体**：Sensor/Motion/Diagnosis/Safety 线程搭建、速度分区阈值诊断算法、黑匣子与 ns_storage 联动
- [ ] **DIR/STEP 实际波形验证**：无示波器，替代方案=TMC 侧回读（读寄存器位置计数随 STEP 变化）或低速信任法（接电机后 1Hz 手动脉冲观察）

## 🟢 低优先 / 比赛材料

- [ ] **BUG-012 上游提交**（等用户发话）：issue + fix PR 草稿已备（调试记录 + BUG-013 归档）；触发语"开始处理 RT-Thread NS800 SPI BSP Bug"，流程 11 步见 BUG-013 §8
- [ ] **扩展板 RevB 修改清单**（下次画板时）：①补 MCU_DRV_ENABLE 网络（漏画）②补完整使能链 Q101/R121/R122/R123 + 急停第一组常闭触点 ③其余见 RevA 方案 §17 检查表
- [ ] **FAE 沟通**：PF.21 焊盘缺陷证据包 6 条目已备（调试记录 BUG-009），可发 Novosense
- [ ] **比赛材料**：BUG-012（官方 BSP 三控制器引脚表全错+真机验证）单独成节写入提交材料
- [ ] **温度/长时间运行测试**：电机冷机/热机基线对比（方案 §14）

## ✅ 最近完成（备查）

- [x] 2026-09-13 Phase 3 IMU 真机验证（SPI3 迁移+DEVID+三轴数据，7aa413a）
- [x] 2026-09-12 TMC2209 读写协议真机全验证（c3dadd8/3c13e48）
- [x] 2026-09-12 BUG-009 换脚迁移 MCU_DRV_ENABLE→PC.23/J4-21（3c1d50b）
- [x] 2026-09-12 BUG-013 归档（d088bbf）

## 硬件在位速查（2026-09-13）

| 器件/线路 | 状态 |
|---|---|
| Flash（spi1, CS=PF.12/J2-12） | ✅ 验证通过 |
| IMU ADXL345（spi3: J3-34/35/36 + CS=J2-14） | ✅ 验证通过 |
| TMC2209（UART2: PDN 桥接 + VM 有电） | ✅ 通信验证；**电机绕组已接、DIR(J4-16)/STEP(J4-18)/DIAG(J4-15) 已接** |
| ENN | 接 3.3V 恒禁用（**唯一安全屏障，插牢**）；MCU_DRV_ENABLE 网络 PCB 漏画，RevB 补 |
| ESTOP_SENSE（J4-40） | 未接（方案已定：急停常闭+上拉） |
| LIMIT_MIN/MAX（J4-19/J4-25） | 暂不做开关；建议 GND 跳线占位 |
| CURRENT_ADC（J1-3） | ✅ 用户自搭采样硬件已接入（拓扑/增益待确认），DIAG(J4-15) 已接 |
